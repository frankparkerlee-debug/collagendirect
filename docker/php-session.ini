; Postgres-backed sessions: register the save handler before every request so
; logins survive container redeploys/restarts (default file sessions live on
; Render's ephemeral disk and are wiped on each deploy).
auto_prepend_file = /var/www/html/api/lib/session_prepend.php
